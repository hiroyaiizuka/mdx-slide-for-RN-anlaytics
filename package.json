{
  "scripts": {
    "start": "fusuma start",
    "build": "fusuma build",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "fusuma pdf"
  },
  "devDependencies": {
    "fusuma": "^1.16.0"
  },
  "dependencies": {
    "styled-components": "^5.2.0"
  }
}
